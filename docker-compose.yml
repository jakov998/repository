version: '3.8'
services:
  db:
    image: postgres:15
    container_name: postgres_db
    environment:
      POSTGRES_DB: betvictor
      POSTGRES_USER: betvictor
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"